import type { Metadata } from "next";

export const metadata: Metadata = { title: "Solve a problem" };

export default function ProblemWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
