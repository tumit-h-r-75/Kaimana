# Set these to the deployed backend URL in Vercel, then redeploy the frontend.
NEXT_PUBLIC_API_URL=http://localhost:5000
# Socket.IO is not supported by Vercel Functions; set this only when a separate
# realtime service has been deployed.
NEXT_PUBLIC_SOCKET_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000
